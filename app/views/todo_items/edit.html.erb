<%= title "Editing Request Item" %>

<%= form_for [@todo_list, @todo_item] do |form| %>
  <%= render partial: form %>
<% end %>

<div class="row">
  <div class="small-12 columns">
    <%= link_to "Delete", todo_list_todo_item_path(@todo_list, @todo_item), method: :delete, data: { confirm: "Are you sure?" } %>
  </div>
</div>